import { LogoStyleDirective } from './logo-style.directive';

describe('LogoStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoStyleDirective();
    expect(directive).toBeTruthy();
  });
});
